{
  "name": "selfwrite",
  "version": "0.2.0",
  "type": "module",
  "description": "Self-improving writing skills for Claude Code (selfwrite, selfresearch, selfinvestigate)",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "lint:skills": "node scripts/skill-lint.mjs",
    "check:run": "node scripts/run-integrity.mjs",
    "audit:run": "node scripts/run-audit.mjs"
  },
  "dependencies": {
    "yaml": "^2.8.1"
  },
  "devDependencies": {
    "vitest": "^2.1.9"
  },
  "engines": {
    "node": ">=20"
  }
}
