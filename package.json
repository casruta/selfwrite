{
  "name": "selfwrite",
  "version": "0.3.0",
  "type": "module",
  "description": "Auditable prose, research, and investigation skills for Claude Code",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "lint:skills": "node scripts/skill-lint.mjs",
    "check:run": "node scripts/run-integrity.mjs",
    "check:evidence": "node scripts/evidence-check.mjs",
    "check:judgment": "node scripts/judgment-check.mjs",
    "audit:run": "node scripts/run-audit.mjs"
  },
  "dependencies": {
    "yaml": "^2.8.1"
  },
  "devDependencies": {
    "vitest": "^2.1.9"
  },
  "engines": {
    "node": ">=20"
  }
}
